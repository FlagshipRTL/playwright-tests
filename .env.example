# Playwright Test Configuration

# Use real brand data instead of UAT test data
# Default: false (uses uat-bocop)

# Options: industry-of-all-nations, twillory-test, public-rec, etc.
BRAND_KEY=twillory-test

# Limit number of products to test (for quick validation)
# Default: all products
# TEST_LIMIT=10

# Number of products to test with full navigation (vs direct URLs)
# Default: 1
# Set to "all" to test all products with navigation
# NAV_TEST_COUNT=5

# PostgreSQL connection for test result ingestion
# Required for: node ingest-results.ts
# DATABASE_URL=postgresql://user:password@host:5432/appdb
